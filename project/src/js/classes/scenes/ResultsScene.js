/* eslint-disable no-undef */
export default class GameScene extends Phaser.Scene {
  constructor() {
    super({
      key: `results`
    });
  }

  init() {
    console.log(`Results scene`);

  }

  onComplete() {
  }

  create() {

  }
}
